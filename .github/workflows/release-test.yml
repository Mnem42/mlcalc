name: test-dev

on:
  release:
    types: [published]

jobs:
    build:
        runs-on: ["ubuntu-latest"]
        steps:
        - uses: actions/checkout@v4
        - name: Build
          run: cargo build --verbose --release